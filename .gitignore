# ---------------------------------------------------------------------------
# This repository holds application code only. Data files live outside version
# control and are supplied separately at runtime.
# ---------------------------------------------------------------------------
# Data files (kept local; never committed).
candidate_variants*.csv
all_samples_fixed.txt
*samples_fixed*
example_variants.csv
app/data/example_variants.csv
example_sample_info.tsv
app/data/example_sample_info.tsv
*.real.csv
*.real.tsv
data/raw/
app/data/raw/
# Generated report folders (kept local; pointed-to at runtime).
igv_reports/
app/data/igv_reports/
by_family*/
*.igv_report.html

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# shiny / rsconnect deployment artefacts
rsconnect/

# OS cruft
.DS_Store
Thumbs.db

# editor / tooling
.vscode/
.claude/

# local working spreadsheets
*.xlsx

# saved filter presets (may embed real sample IDs; kept local, never committed)
app/data/.saved_filters.json
