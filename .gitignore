# ---------------------------------------------------------------------------
# PRIVACY: never commit real participant / family-level data
# Only the de-identified example_variants.csv is allowed in the repo.
# ---------------------------------------------------------------------------
# Match real-data files wherever they live (top level or app/data/).
candidate_variants*.csv
all_samples_fixed.txt
*samples_fixed*
*.real.csv
*.real.tsv
data/raw/
app/data/raw/

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# shiny / rsconnect deployment artefacts
rsconnect/

# OS cruft
.DS_Store
Thumbs.db

# editor / tooling
.vscode/
.claude/

# local working files (source spreadsheets; the committed .tsv is canonical)
*.xlsx
